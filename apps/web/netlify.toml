[build]
  command = "npm run build && npx ng run web:prerender"
  publish = "dist/browser"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
